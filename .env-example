# Copy this file to one of the following depending on your environment:
# - .env (shared defaults)
# - .env.local (local-only overrides, not committed)
# - .env.development (dev environment)
# - .env.production (prod environment)
# - .env.test (test environment)
#
# Vite only exposes variables prefixed with VITE_.
# Access them via import.meta.env.VITE_* in code.

# App metadata
VITE_APP_NAME="Frontend S1"
VITE_APP_ENV=development

# API configuration
# Example: https://api.example.com or http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000

# Feature flags
VITE_ENABLE_MOCKS=false

# Analytics (example)
VITE_ANALYTICS_WRITE_KEY=

# Sentry/Monitoring (example)
VITE_SENTRY_DSN=
